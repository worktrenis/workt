This archive was generated by scripts/backup-with-notes.ps1 on 03/07/2026 10:02:40.

To restore the backup:

1. Unzip the file somewhere safe, e.g. unzip workt-backup-*.zip -d workt-restored.
2. Copy the extracted folder to your working location or open it with VS Code.
3. If using yarn/npm, run 
pm install or yarn to restore dependencies.
4. If you are on Windows and using Expo, you may also need to run expo prebuild 
   or other project-specific commands as documented in the project README.
5. Remember to remove any sensitive or environment-specific files before committing.

NOTE: This archive does not include node_modules, the .git folder, or backups,
      so you will need to run 
pm install/yarn install after restoring.

Happy coding!
